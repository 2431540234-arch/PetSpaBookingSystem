server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petspa?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: doi_thanh_password_mysql_cua_ban
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # Flyway quan ly schema, khong de Hibernate tu tao bang
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  jwt:
    secret: "doi-chuoi-bi-mat-nay-thanh-chuoi-dai-it-nhat-32-ky-tu-truoc-khi-deploy-thuc-te"
    expiration-ms: 86400000   # 24 gio
